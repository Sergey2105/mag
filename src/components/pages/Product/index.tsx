import { getProducts } from "@/lib/db/getProducts";

export default async function Product(props: any) {
    const { id } = props;
    // const products = await getProductByID(id);

    return <div className="wrapper mt-10">asd</div>;
}
